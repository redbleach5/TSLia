export type State = 'idle'|'listening'|'thinking'|'speaking'|'error'
export type Message = {id:string; role:'user'|'assistant'|'system'; text:string; time:string}
export type ServerEvent = {type:'state'; state:State} | {type:'transcript'; text:string; final?:boolean} | {type:'partial_transcript'; text:string; request_id?:number} | {type:'assistant_chunk'; text:string; reply_id:string} | {type:'assistant_text'; text:string; reply_id?:string} | {type:'audio'; data:string; mime:string; sampleRate?:number; reply_id?:string; sentence?:string; index?:number} | {type:'cancelled'; reply_id?:string} | {type:'error'; message:string}