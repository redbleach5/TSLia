export type State = 'idle'|'listening'|'thinking'|'speaking'|'error'
export type Message = {id:string; role:'user'|'assistant'|'system'; text:string; time:string}
export type ServerEvent = {type:'state'; state:State} | {type:'transcript'; text:string; final?:boolean} | {type:'assistant_text'; text:string} | {type:'audio'; data:string; mime:string; sampleRate?:number} | {type:'error'; message:string}